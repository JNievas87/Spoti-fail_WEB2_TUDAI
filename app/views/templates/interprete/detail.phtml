<?php require_once __DIR__ . '/../layout/header.phtml'; ?>

<div class="container mt-5">
    <div class="card shadow-lg border-0 overflow-hidden">
        <div class="row g-0">
            <div class="col-md-4">
                <?php if (!empty($interprete->Imagen)): ?>
                    <img src="<?= $interprete->Imagen ?>"
                         alt="<?= $interprete->Nombre ?>"
                         class="img-fluid h-100 interprete-detail-img">
                <?php endif; ?>
            </div>

            <div class="col-md-8">
                <div class="card-body p-5">
                    <h1 class="display-5 fw-bold text-success"><?= $interprete->Nombre ?></h1>
                    <hr>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item"><strong>Género:</strong> <?= $interprete->Genero ?></li>
                        <li class="list-group-item"><strong>Tipo:</strong> <?= $interprete->Tipo ?></li>
                        <li class="list-group-item"><strong>País de origen:</strong> <?= $interprete->Pais_Origen ?></li>
                        <li class="list-group-item"><strong>Sello discográfico:</strong> <?= $interprete->SelloDiscografico ?></li>
                        <li class="list-group-item"><strong>Sitio web:</strong> 
                            <a href="<?= $interprete->SitioWeb ?>" target="_blank"><?= $interprete->SitioWeb ?></a>
                        </li>
                        <li class="list-group-item"><strong>Fecha de inicio:</strong> <?= $interprete->FechaInicio ?></li>
                    </ul>

                    <div class="mt-4">
                        <a href="<?= BASE_URL ?>interprete" class="btn btn-outline-secondary">← Volver a intérpretes</a>
                        <?php if($user): ?>
                            <a href="<?= BASE_URL ?>editInterprete/<?= $interprete->ID_Interprete ?>" class="btn btn-warning">Editar</a>
                            <a href="<?= BASE_URL ?>deleteInterprete/<?= $interprete->ID_Interprete ?>" class="btn btn-danger">Eliminar</a>
                        <?php endif; ?>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="mt-5">
        <h2 class="text-success">Canciones</h2>
        <hr>
        <?php if(empty($canciones)): ?>
            <p class="text-muted">Este intérprete no tiene canciones cargadas.</p>
        <?php else: ?>
            <div class="row">
                <?php foreach($canciones as $cancion): ?>
                    <div class="col-12 col-md-4 mb-3">
                        <div class="card shadow-sm">
                            <img src="<?= $cancion->Portada ?>" 
                                 class="card-img-top" 
                                 style="height: 150px; object-fit: contain;">
                            <div class="card-body">
                                <h5 class="card-title"><?= $cancion->Titulo ?></h5>
                                <p class="card-text small text-muted"><?= $cancion->Album ?></p>
                                <a href="<?= BASE_URL ?>cancion/<?= $cancion->ID_Cancion ?>" class="btn btn-primary btn-sm">Ver detalle</a>
                            </div>
                        </div>
                    </div>
                <?php endforeach; ?>
            </div>
        <?php endif; ?>
    </div>
</div>

<?php require_once __DIR__ . '/../layout/footer.phtml'; ?>