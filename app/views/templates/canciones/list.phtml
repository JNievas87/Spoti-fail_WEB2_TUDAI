<?php require_once __DIR__ . '/../layout/header.phtml'; ?>

	<main class="container mt-4">
		<div class="d-flex justify-content-between align-items-center mb-4">
			<h1>Lista de Canciones</h1>
            <?php if($this->user): ?>
			    <a href="<?= BASE_URL ?>cancion/add" class="btn btn-success"> + Agregar Canción</a>
            <?php endif; ?>
		</div>

		<div class="row">
            <?php foreach($canciones as $cancion): ?>
				<div class="col-12 col-sm-6 col-lg-3 mb-4"> <div class="card h-100 shadow-sm border-0">
						<img src="<?= $cancion->Portada ?>" class="card-img-top" style="height: 150px; object-fit: contain; background: #f8f9fa;">

						<div class="card-body p-3">
							<h5 class="card-title h6 text-truncate" title="<?= $cancion->Titulo ?>">
                                <?= $cancion->Titulo ?>
							</h5>
							<p class="card-text small text-muted mb-1">
                                <?= $cancion->Nombre_Artista ?> </p>
							<p class="badge bg-info text-dark mb-2" style="font-size: 0.7rem;">
                                <?= $cancion->Genero ?>
							</p>

							<div class="d-grid mt-2">
							    <a href="<?= BASE_URL ?>cancion/<?= $cancion->ID_Cancion ?>" class="btn btn-primary btn-sm">Ver Detalle</a>
							</div>
						</div>
					</div>
				</div>
            <?php endforeach; ?>
		</div>
	</main>

<?php require_once __DIR__ . '/../layout/footer.phtml'; ?>