<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
     <?php // BASE_URL se genera dinámicamente en config.php según la ruta del servidor ?>
    <base href="<?= BASE_URL ?>">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="<?= BASE_URL ?>app/assets/style.css">

    <title>Spoti-Fail</title>
</head>

<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
        <a class="navbar-brand" href="<?= BASE_URL ?>home/">🎵 Spoti-Fail</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item"><a class="nav-link" href="<?= BASE_URL ?>cancion">Canciones</a></li>
                <li class="nav-item"><a class="nav-link" href="<?= BASE_URL ?>interprete">Intérpretes</a></li>
            </ul>
            <div class="d-flex align-items-center">
                <?php if (isset($this->user) && $this->user): ?>
                    <span class="navbar-text me-3">Hola, <strong><?= $this->user->user ?></strong></span>
                    <a href="logout" class="btn btn-outline-danger btn-sm">Salir</a>
                <?php else: ?>
                    <button type="button" class="btn btn-outline-success btn-sm" data-bs-toggle="modal" data-bs-target="#loginModal">
                        Ingresar
                    </button>
                <?php endif; ?>
            </div>
        </div>
    </div>
</nav>

<?php require_once __DIR__ . '/loginModal.phtml'; ?>

<?php if (!empty($_SESSION['login_error'])): ?>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            var modal = new bootstrap.Modal(document.getElementById('loginModal'));
            modal.show();
        });
    </script>
    <?php unset($_SESSION['login_error']); ?>
<?php endif; ?>

<main class="container shadow-sm p-4 bg-white rounded">